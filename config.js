/* Optional settings — safe to edit directly on GitHub, no coding needed.

   RESULTS_WEBHOOK_URL: paste your Google Apps Script Web App URL here
   (see sheet-logger-Code.gs for the script + setup steps) to turn on
   auto-logging — every completed attempt is automatically appended as a
   row in a Google Sheet, with no button or action needed from the student.

   Leave it as an empty string ("") and the site still works — the
   certificate still shows, and students are told to show their certificate
   (or its verification code) to their teacher directly.
*/
const RESULTS_WEBHOOK_URL = "https://script.google.com/macros/s/AKfycbx30UNIm9ZB-prijn2rSSMz_AYDPIGZmUJzad0naQGUgXubeOyKDSSKvra9YwYCCBSX/exec";
